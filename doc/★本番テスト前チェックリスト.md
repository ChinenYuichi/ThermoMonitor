# ThermoMonitor 本番テスト前チェックリスト

対象バージョン：v2.9.3  
作成日：2026-05-05

---

## 事前疎通確認済み（2026-05-05 確認）

| 確認項目 | 結果 |
|---|---|
| Modbusサーバー `192.168.52.248:502` | **接続OK**（TCP接続成功） |
| `csv02.exp.triam.kyushu-u.ac.jp/expinfo/time.txt` | **取得OK**（HTTP 200） |
| `time.txt` の値 | `-36000.0`（待機中・正常） |

---

## チェックリスト

### ネットワーク

- [ ] `ping 192.168.52.248` が通る（損失0%）
- [ ] `Test-NetConnection -ComputerName 192.168.52.248 -Port 502` が True

```powershell
ping 192.168.52.248
```

```powershell
Test-NetConnection -ComputerName 192.168.52.248 -Port 502
```

### hostsファイル（ローカルテスト用エントリの残存確認）

- [ ] 以下のコマンドで何も表示されないこと

```powershell
Select-String -Path "C:\Windows\System32\drivers\etc\hosts" -Pattern "csv02"
```

表示された場合はテキストエディタ（管理者権限）で以下の行を削除する。

```
127.0.0.1 csv02.exp.triam.kyushu-u.ac.jp
```

### ショット時刻サーバー

- [ ] `time.txt` が取得できること（値は `-1000` 以下なら待機中・正常）

```powershell
Invoke-WebRequest -Uri 'http://csv02.exp.triam.kyushu-u.ac.jp/expinfo/time.txt' -UseBasicParsing | Select-Object -ExpandProperty Content
```

---

## ThermoMonitor 起動・接続

- [ ] `ThermoMonitor.exe` を起動
- [ ] IPアドレス：`192.168.52.248`、ポート：`502` であることを確認
- [ ] 「接続」ボタンを押し、ログに `接続成功: 192.168.52.248:502` が表示される

---

## 本番テストで確認する項目

- [ ] 温度グラフ（4CH）が更新されている
- [ ] 実ショット時に `[SHOT START]` がログに記録される
- [ ] ショット終了後に `[SHOT STOP]` とピーク温度がログに記録される
- [ ] `-12 (No connection established)` エラーが**出ない**

ログファイル：`C:\Users\quest\ThermoMonitor_log.txt`

```powershell
Get-Content -Path C:\Users\quest\ThermoMonitor_log.txt -Tail 20
```

---

## 参照ドキュメント

- [本番環境確認手順書.md](本番環境確認手順書.md) — 詳細な手順・異常時対応
- [ローカルテスト手順書.md](ローカルテスト手順書.md) — ローカル環境での再テスト手順
